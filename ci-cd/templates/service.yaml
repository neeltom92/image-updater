apiVersion: v1
kind: Service
metadata:
  name: {{ include "ci-cd.fullname" . }}
  labels:
    {{- include "ci-cd.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ci-cd.selectorLabels" . | nindent 4 }}
